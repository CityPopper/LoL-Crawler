# Rate-limiter service uses the unified Dockerfile.service.
# Build via docker-compose (which sets SERVICE_NAME / MODULE_NAME args).
#
# Standalone build:
#   docker build --build-arg SERVICE_NAME=rate-limiter --build-arg MODULE_NAME=lol_rate_limiter \
#       -f Dockerfile.service .
